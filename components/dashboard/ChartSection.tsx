import { View, Text, StyleSheet } from "react-native";

export function ChartSection() {
  return (
    <View style={styles.container}>
      <Text>Graphique à venir</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    marginBottom: 16,
  },
});
